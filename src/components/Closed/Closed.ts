import classes from './Closed.module.scss';

export default function Closed() {
  return /* HTML */ `
    <div class=${classes.wrapper} id="terminal-closed">
      omg wtf why did you close the window :(
    </div>
  `;
}
